<h1>Editing comment</h1>

<%= turbo_frame_tag @comment do %>
  <%= render "form", comment: @comment %>
  <div><%= link_to 'Back', user_path(@comment.post.author),
    data: { turbo_frame: "_top" } %></div>
<% end %>

<br>



<%= form_with model: @user,
  url: update_profile_picture_user_path(@user),
  method: :patch,
  local: true do |form| %>
  <div>
    <%= form.label :profile_picture, :image do %>
      <%= image_tag "attach-image.svg", alt: "attach image"  %>
    <% end %>
    <%= form.file_field :profile_picture,
    data: { 'form-target': 'fileField' }, 'upload-image-file-field': 'change->upload-image#filePicked'  %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
